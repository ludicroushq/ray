[hooks]
enter = '''
if [ -x "$MISE_PROJECT_ROOT/node_modules/.bin/ruler" ]; then
  bun --cwd "$MISE_PROJECT_ROOT" --silent ruler
fi
'''

[tools]
bun = "1.3.14"
node = "24.13.0"

[settings]
experimental = true

[env]
_.file = [".env", ".env.local"]
_.path = ["node_modules/.bin"]
